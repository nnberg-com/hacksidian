# Приёмы для плагинов

[[atlas/atlas|Атлас приёмов]]

- [[atlas/! plugins/horizontal-blocks|Horizontal Blocks]] — колонки Markdown.

Страницы наборов находятся в `! plugins`, приёмы — в `! hacks/plugin-[название плагина]-[код приёма]`. Применённый CSS записывается в `hacksidian-plugin-[название плагина].css`.
