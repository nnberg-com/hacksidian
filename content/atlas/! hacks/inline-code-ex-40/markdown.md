---
cssclasses:
  - atlas-inline-code-ex-40
---

Большая подложка у `fontSize` показывает, как декор заходит на пространство соседних строк. Несколько фрагментов `lineHeight` и `padding` делают эффект заметнее в узкой колонке.
