---
tags:
  - hacksidian_technique
  - hacksidian_metadata
title: Счётчик отмеченных свойств
category: metadata
sources:
  - https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Selectors/:has
format: properties
themes: []
---

```hacksidian-id
metadata-count
```

```hacksidian-live
metadata-count
```

HTML-модель панели свойств или YAML-редактора. Можно менять значения, флажки и фокус внутри примера; заметка не изменяется. Переключатель приёма показывает эффект актуального CSS.


```hacksidian-details
> Зачем
Быстро увидеть, сколько флажков включено в небольшом контрольном наборе.

> Как работает
CSS counter сбрасывается на контейнере и увеличивается на отмеченных строках; итог выводится псевдоэлементом после формы.

> Ограничения
Демонстрация моделирует разметку Properties или YAML, а не запускает настоящий редактор Obsidian. Поведение в самой заметке зависит от её режима и темы.
```
```hacksidian-sources
metadata-count
```

```hacksidian-files
metadata-count
```
