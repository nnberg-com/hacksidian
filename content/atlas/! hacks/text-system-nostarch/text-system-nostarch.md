---
tags:
  - hacksidian_technique
  - hacksidian_text-systems
title: No Starch Press — The Modern Web
category: text-systems
sources:
  - "https://nostarch.com/download/samples/modernweb_ch6.pdf#page=6"
format: markdown
themes: []
---

```hacksidian-id
text-system-nostarch
```

```hacksidian-live
text-system-nostarch
```

```hacksidian-details
> Зачем
Согласовать масштаб заголовков, насыщенность, межстрочные интервалы и расстояние до текста в одной принятой композиции.

> Как работает
Принято · PDF, стр. 111 · один уровень; размеры измерены, отбивки приблизительны. Все размеры пропорциональны пользовательскому `--font-text-size`. Интерлиньяж текста — 1.2. Используются штатные `--line-height-normal`, `--hN-size`, `--hN-line-height`, `--hN-weight`, `--hN-style`, `--heading-spacing` и `--p-spacing`. Последние две переменные задаются локально на заголовках. Собственных CSS Variables и назначений гарнитур нет. Приём записывается в существующий текстовый сниппет `g-text`.

> Ограничения
Уровни H2; остальные заголовки сохраняют оформление темы. Применяйте одну текстовую систему за раз и отключайте предыдущую: иначе CSS разных систем смешивается. Гарнитуры, базовый размер текста, ширина строки, списки и оформление абзацев не назначаются. Отбивки соседних с заголовками абзацев обнуляются для точного короткого зазора. В Live Preview пустые строки Markdown сохраняются, поэтому интервалы могут отличаться от режима чтения. Тема и другие приёмы могут переопределять правила. Это реконструкция конкретного образца книги, не универсальный стандарт издательства; отбивки и интерлиньяж заголовков приблизительны.
```

```hacksidian-sources
text-system-nostarch
```

```hacksidian-files
text-system-nostarch
```
