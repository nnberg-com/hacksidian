---
cssclasses:
  - atlas-code-e033
---

> [!code-line|1,3-5,8]
> ```
> const palette = {
>   background: "#f8f7f2",
>   accent: "#bd5036",
>   spacing: [4, 8, 16]
> };
>
> // Готово к выводу
> render(palette);
> ```

Подсвечены строки 1, 3–5 и 8. Измените список после `code-line|`, чтобы выбрать другие строки.
