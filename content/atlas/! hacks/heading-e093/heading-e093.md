---
tags:
  - hacksidian_technique
  - hacksidian_heading
title: Мягкая маска
category: heading
sources:
  - https://developer.mozilla.org/en-US/docs/Web/CSS/mask-image
format: markdown
themes: []
---

```hacksidian-id
heading-e093
```

```hacksidian-live
heading-e093
```

```hacksidian-details
> Зачем
Часть букв постепенно растворяется без жёсткого обрезания.

> Как работает
mask-image с градиентом управляет альфа-прозрачностью всего заголовка.

> Ограничения
Чисто декоративный эффект; слабая прозрачность ухудшает читаемость.
```

Параметр «Уровень заголовка» выбирает H1–H6 в Preview mode. Пример перестраивается под выбранный уровень; исходная заметка не изменяется. Остальные параметры настраиваются независимо.

```hacksidian-sources
heading-e093
```

```hacksidian-files
heading-e093
```
