---
tags:
  - hacksidian_technique
  - hacksidian_iframe
title: Цвет появляется при наведении
category: iframe
sources: []
format: html
themes: []
---

```hacksidian-id
iframe-e031
```

```hacksidian-details
> Зачем
Превью, которые не должны спорить с текстом за внимание.

> Как работает
Hover и фокус на обёртке возвращают цвет. На сенсорном экране цвет включён сразу.

CSS заметки оформляет внешний элемент iframe; содержимое встроенного документа имеет собственные стили.

> Ограничения
Внутренний фокус iframe не следует считать обычным DOM-потомком: обёртка имеет собственный tabindex.

CSS снаружи iframe не меняет фон, прокрутку и содержимое вложенной страницы. `height: auto` не подгоняет рамку под её содержимое; `object-fit` не кадрирует iframe как картинку.

Живой пример пока не готов: образец содержит iframe или интерактивный HTML, для которого ещё не реализован отдельный просмотрщик.
```
```hacksidian-sources
iframe-e031
```


```hacksidian-files
iframe-e031
```
