---
tags:
  - hacksidian_technique
  - hacksidian_metadata
title: Точка отсчёта
category: metadata
sources:
  - https://github.com/obsidianmd/obsidian-developer-docs/blob/main/en/Reference/CSS%20variables/Editor/Properties.md
format: properties
themes: []
---

```hacksidian-id
metadata-baseline
```

```hacksidian-live
metadata-baseline
```

HTML-модель панели свойств или YAML-редактора. Можно менять значения, флажки и фокус внутри примера; заметка не изменяется. Переключатель приёма показывает эффект актуального CSS.


```hacksidian-details
> Зачем
Сравнить все варианты с обычной формой: название слева, значение справа.

> Как работает
Общий учебный слой повторяет основные классы Properties; этот пример не добавляет локальных изменений.

> Ограничения
Это точка отсчёта: если текущий интервал уже равен 3px, включение и выключение не меняет вид.

Демонстрация моделирует разметку Properties или YAML, а не запускает настоящий редактор Obsidian. Поведение в самой заметке зависит от её режима и темы.
```
```hacksidian-sources
metadata-baseline
```

```hacksidian-files
metadata-baseline
```
