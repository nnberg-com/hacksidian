---
cssclasses:
  - atlas-inline-code-ex-75
---

### Параметр `timeout`

Обычный `timeout`, **важный `timeout`**, *выделенный `timeout`*, ссылка [`справка`](#sources).

> Значение `3000` задаётся в миллисекундах.

- `0` — без ожидания
- `3000` — три секунды

```
{ "timeout": 3000 }
```
