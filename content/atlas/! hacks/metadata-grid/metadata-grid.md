---
tags:
  - hacksidian_technique
  - hacksidian_metadata
title: Автоматическая сетка
category: metadata
sources:
  - https://forum.obsidian.md/t/css-double-columns-property/103543
format: properties
themes: []
---

```hacksidian-id
metadata-grid
```

```hacksidian-live
metadata-grid
```

HTML-модель панели свойств или YAML-редактора. Можно менять значения, флажки и фокус внутри примера; заметка не изменяется. Переключатель приёма показывает эффект актуального CSS.


```hacksidian-details
> Зачем
Расположить короткие свойства в две колонки, если для них хватает места.

> Как работает
repeat(auto-fit, minmax(...)) сам выбирает число колонок. На узкой панели остаётся одна.

> Ограничения
Демонстрация моделирует разметку Properties или YAML, а не запускает настоящий редактор Obsidian. Поведение в самой заметке зависит от её режима и темы.
```
```hacksidian-sources
metadata-grid
```

```hacksidian-files
metadata-grid
```
