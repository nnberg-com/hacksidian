# Продолжение аудита: 7 сентября 2026

## Восстановление P0

В vault `/Users/op/vaults/callmered` применён полный CSS: 94 правила, 26 216 байт. Все исходные правила и декларации полной версии сохранены в исходном порядке. Добавлены только 11 переменных `--code-*` и пять правил Prism/Highlight.js из последнего решения по палитре.

Воспроизводимая сборка: `node scripts/recover-style.mjs` из каталога `poc`. Источники зафиксированы в `style-recovery/2026-09-07/full-before.css` и `palette-before.css`, результат — `restored.css`. Скрипт не читает изменяемый активный сниппет и не применяет результат самостоятельно.

Применение проведено через загруженный плагин: обновлены runtime style, snippet, state и история версий; сохранён Undo. ID восстановления: `0247e9f5-0e67-41ff-8bd8-3a8f112b0bf9`. Это техническая версия `recovery`, а не выдуманная реакция пользователя или LLM-ход. Пути снимков до/после находятся в `style-recovery/2026-09-07/application.json`.

## Первая защита от повторения регрессии

Перед применением ответа LLM `validateCssPreservation` сравнивает покрытия селекторов и их свойств с учётом контекста at-rules. Исчезновение селектора, свойства или custom property отклоняет весь ответ до изменения активного CSS. Значения менять можно, новые правила добавлять можно. Prompt обновлён до `poc-2-preservation`. Обновлённый плагин собран, скопирован в vault и перезагружен.

Это консервативный промежуточный барьер, **не полная реализация P1**. Он не защищает значения соседних компонентов, не обнаруживает все способы нейтрализации правил каскадом и намеренно отклоняет часть допустимых структурных рефакторингов. LLM пока возвращает полный stylesheet. Модульного хранения и локальных структурированных изменений ещё нет.

## Проверка

`npm run check`: TypeScript, 27 тестов, production build прошли. Новый regression test отклоняет фактическую потерю CSS от 3 сентября. Другой тест доказывает точное сохранение базового CSS после удаления исключительно перенесённых syntax rules и variables.

В Obsidian 1.13.7 проверены runtime CSS и computed styles на обзорной статье, списках/задачах, выносках, коде и формулах/сносках. На обзорной статье обнаружены все восемь базовых компонентов из P0. Результаты: `style-recovery/2026-09-07/runtime-verification.json`. На измеренной ширине Reading view 881 px `scrollWidth` совпал с `clientWidth` на всех пяти страницах; это не проверка всех ширин и всех состояний.

Настоящий скриншот окна `style-recovery/2026-09-07/obsidian-callouts.png` просмотрен: присутствуют иконки выносок на полях и цветные линии справа. Пользовательская визуальная приёмка ещё не получена.

Встроенный `html-to-image` захват плагина дал большой пустой участок в снимке обзорной статьи. Такой снимок нельзя считать надёжным отражением экрана. Это отдельная незакрытая проблема захвата; для визуальной проверки использован Obsidian `dev:screenshot`.

## Следующая точка

Продолжить P1: компонентное внутреннее хранение с сохранением порядка каскада, детерминированная сборка и структурированные изменения выбранного модуля. Не считать нынешний guard заменой проверки неизменности соседних модулей. Перед новыми визуальными LLM-итерациями также разобраться с недостоверным встроенным захватом.

На момент завершения P0 пункты P2–P5 не выполнялись. Последующая работа над P2 описана ниже.

## Качество CSS: начало P2

По следующему запросу пользователя выполнена нормализация через `node scripts/normalize-style.mjs`. Вход — сохранённый `restored.css`, результат — `style-recovery/2026-09-07/normalized.css`.

- Точные hex-цвета вынесены в собственные raw tokens; прежние имена сохранены как ссылки на них. Это начало разделения уровней из пункта 3, не полная семантическая модель.
- Onest и Meslo определены по одному разу. Объявления `font-family` и адаптерные переменные Obsidian используют собственные `--cmr-font-text` и `--cmr-font-code`.
- Повторения базовых 18 px и line-height 1.5 заменены собственными переменными. Индивидуальные размеры и интервалы заголовков сохранены.
- Поле страницы явно равно 72 px и больше не вычисляется из размера callout icon (пункт 7).
- Obsidian `--code-*` и правила Prism/Highlight.js направлены на один набор syntax tokens (пункт 15). Сохранены существующие пять цветовых групп.

TypeScript, 28 тестов и build прошли. Тест проверяет эквивалентность разрешённых значений деклараций, порядок селекторов и `!important`. Runtime-проверка сравнила 6041 состояние элементов на пяти страницах и пяти позициях прокрутки, включая `::before`, `::after`, `::marker`, по 20 свойствам. Отличий нет. Это выборочная проверка вычисленных стилей, не полная визуальная приёмка и не проверка всех ширин.

Результаты: `normalization-verification.json`. Изменение применено после успешного сравнения, ID `50dba8ac-45c5-43af-a85a-9f5d965b5f5e`; Undo сохранён. `normalization-application.json` отмечает технический refactor без пользовательской визуальной реакции.

Из runtime установлено, что текущий `--radius-l` равен **12 px**. Предложенные в исходном аудите 8 px не соответствуют нынешней картинке. Радиусы пока не изменены; для устранения внешней зависимости нужно зафиксировать наблюдаемые 12 px, а любое уменьшение проводить отдельной визуальной итерацией.

Остаются: полная модульность P1; завершение разделения semantic/component tokens; собственные радиусы; независимая типографика inline code/mark/tag; обоснованное сокращение `!important` и DOM-дублей P5; исправление захвата. Пункты P3–P4 также не закрыты.

## Следующая чистка: радиусы и inline-компоненты

`node scripts/clean-style.mjs` берёт `normalized.css` и воспроизводимо собирает `cleaned.css`.

- Удалена зависимость от Obsidian `--radius-l`: собственный `--cmr-radius-inline` равен наблюдаемым 12 px. Базовый радиус по-прежнему 4 px; новое отношение между радиусами не придумано.
- Общий inline-селектор отвечает только за геометрию. Типографика inline code, mark и tag выделена в отдельные правила с собственными ссылками на шрифт. Все три пока сохраняют Meslo, а размер и line-height — `inherit`. Теперь изменение mark не требует переписывать типографику кода и тегов.
- Повторяющийся heading line-height 1.12 вынесен в собственный token; индивидуальные формулы H3–H6 сохранены.
- Повторяемая runtime-проверка вынесена в общий `scripts/verify-style-refactor.js`; два входных скрипта задают версии для сравнения. Перед сохранением проверяются неизменность runtime CSS и совпадение сниппета с исходной версией.

TypeScript, 30 тестов и build прошли. Runtime-проверка семи раскрасок в пяти позициях прокрутки сравнила 7123 состояния элементов, включая псевдоэлементы, по прежним 20 свойствам. Отличий нет: `cleanup-verification.json`. Проверка не заменяет пользовательскую визуальную приёмку и не охватывает все состояния/размеры окна.

Применено с Undo, ID `613b002e-f038-4d74-8278-ae06651bddbe`; запись — `cleanup-application.json`. Текущий активный результат — `cleaned.css`.

Следующий крупный технический шаг — P1: модульная сборка и изменения выбранных модулей вместо ответа полным CSS. P5 (DOM-дубли и `!important`) требует отдельной проверки специфичности и состояний; механического удаления не проводилось. Проблема снимков также остаётся открытой.

## P1: модульная сборка подключена

Созданы 17 упорядоченных фрагментов в `poc/style/` с manifest. `npm run style:build` после сборки инструментов создаёт `dist/callmered-current.css`. Проверка `cmp` подтверждает байтовое совпадение с `cleaned.css`.

`src/style-modules.ts` импортирует старый CSS без перестановки правил, собирает модули и проверяет структурный контракт замены. Повторные сегменты одного компонента сохраняют позиции в каскаде. При локальной замене остальные сегменты остаются байт в байт прежними. Селекторы, порядок правил, условия at-rules, свойства и флаги `!important` модели менять нельзя; можно менять значения. Начальный пустой stylesheet допускает создание первых правил, после чего также получает контракт.

Provider/schema/prompt переключены на `moduleId` + CSS одного модуля (`poc-3-modules`). Полный монолит в ответе отклоняется. Сохранение и Undo работают с `state.style`; прежние версии импортируются при необходимости. В vault экспортируются отдельные модули и manifest под `CallMeRed/Style/<hash>/`. При запросе, устаревшем из-за изменения активного стиля, ответ не применяется.

Проверки: TypeScript, 35 тестов, build. Runtime-отчёт `style-recovery/2026-09-07/modular-verification.json` подтверждает миграцию без изменения CSS, отклонение монолита без изменения состояния, неизменность соседей, восстановление модулей после Undo и совпадение сборки файлов vault. Для проверки commit/Undo изменялся только комментарий; настоящего LLM-вызова не выполнялось. Обновлённый плагин установлен и перезагружен.

Ограничения первой версии: глобальные tokens/page остаются общим `foundation`; выбор модуля делает модель, программа фиксирует границы выбранного фрагмента. Изменение shared token может влиять на другие компоненты, граф таких зависимостей ещё не вычисляется. Добавление новых компонентов и структурная перестройка выполняются технически. Дефект захвата изображения и P5 остаются отдельными задачами.
