- [x] Add a separate structured response and prompt grounded in current CSS parameters.
- [x] Route card requests without requiring the catalog; preserve recommendation search.
- [x] Validate and apply changes atomically through the existing form write queue.
- [x] Reject stale pages and changed CSS before writing.
- [x] Record actual before/after values and expenses, including rejected paid responses.
- [x] Add automated tests for provider contract, bounds, atomicity, context, recommendation fallback and costs.
- [ ] Verify real model interpretation and rendered form/preview updates in installed Obsidian. No paid live request or GUI verification performed by the agent.

- [x] Return parameter presets in the same response as a uniquely grounded recommendation.
- [x] Supply current local field definitions without requiring remote catalog resynchronization.
- [x] Test the thick dashed border example, single-request search flow, stale CSS, missing fields and ambiguous alternatives.

- [x] Classify show/apply/uncertain separately from parameter changes, grounded in the current request.
- [x] Apply or update one technique through the shared button mechanism; open a unique search result in a new active tab.
- [x] Force multiple results to list-only behavior and test the table-e015 maximum command and subsequent update.
