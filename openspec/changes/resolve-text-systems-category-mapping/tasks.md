- [x] Уточнить полный охват H1–H6 и разрешение явно обозначенных адаптаций.
- [x] Проверить первоисточники, записать значения и происхождение.
- [x] Реализовать общую схему параметров с таблицами значений.
- [x] Свести text-systems в один приём; перенести expanded и навигацию.
- [x] Свести светлые palette в один приём и expanded.
- [x] Добавить общий рендер expanded, ссылку и атомарное сохранение выбора.
- [x] Удалить тёмные ветки и зафиксировать отдельную будущую задачу.
- [x] Завершить автоматические проверки и проверку браузерного стенда.
- [ ] Получить визуальную приёмку в установленном Obsidian; авторский vault не трогать без отдельного поручения.

## Проверка 2026-09-23

- `npm --prefix plugin run check`: typecheck/build и 275 тестов, 50 файлов — успешно.
- `verify-card-format`: 1388 карточек (включая существующую вложенную карточку); канонический каталог рецептов содержит 1387 приёмов.
- `verify-recipe-catalog`: все 1387 рецептов компилируются и повторно применяются идемпотентно; обычные CSS сохраняются побайтово, две таблицы вариантов компилируются в стандартный CSS.
- `git diff --check`: успешно.
- `openspec validate resolve-text-systems-category-mapping --strict` и `openspec validate add-dark-appearance --strict`: успешно.
- In-app Chromium, изолированный браузерный стенд с реальными LiveExample/ExpandedExample/ParameterControls и моделью API Obsidian: проверены вычисленные стили 60 заголовков, 11 светлых палитр, отсутствие записей при просмотре, сохранение выбора expanded → обычный select, отсутствие влияния выбора на соседние примеры и внешнюю страницу. Ровно три записи для трёх явных действий. После unload осталось 0 подписок. В консоли нет ошибок.
- Визуально осмотрены Carbon и Solarized. Это не проверка нативного MarkdownRenderer/Live Preview в установленном Obsidian.
- Стенд воспроизводится командой `node plugin/tests/browser/expanded/build.mjs /private/tmp/hacksidian-expanded-browser`, затем локальным HTTP-сервером и кнопкой «Проверить сценарии».
- Поиск по CSS/HTML текущего content не обнаружил `theme-dark`, `local-dark`, `#dark`, тёмных `prefers-color-scheme` и `color-scheme`. Специальная PaletteGallery и механизм exclusiveGroup удалены.
- Генераторы проверены во временном дереве: сохранение выбора, повторяемость результата и отказ при исчезнувшем выбранном значении.
- Авторский vault, настройки, установленный плагин и удалённый поисковый индекс не изменялись. Сборка находится в plugin/dist; для нативной проверки нужна отдельная установка.
